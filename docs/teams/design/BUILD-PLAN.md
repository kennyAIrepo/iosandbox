# BUILD-PLAN — `teamslab.html` in three waves, at most 8 parallel builders

Read `CONTRACTS.md` first (exact APIs, per-frame flow, packets, checkpoints); `SPEC.md` for behaviour and UX. Paths:
`R` = `C:/Users/hanna/AppData/Local/Temp/claude/c--Users-hanna-iosandbox/31746f0b-c4ab-4b4f-9c24-629c6e7b40fd/scratchpad/research`,
`REPO` = `C:/Users/hanna/iosandbox`. Every task: only the files it lists; never edit `sdk/core/*`, `sdk/interaction/*`,
`mpbrowser.html`, anything avatar-rig; re-run `npm run test:twin` before declaring done; acceptance checks run headless
from `REPO` (`node tests/<x>.mjs`, or `npm run serve` on :3333 + a puppeteer probe with Chrome's fake camera).

```
wave 0   T0 ──────────────────────────────────────────────────────────────┐
wave 1   T1 ui   T2 stage/tiles   T3 room/bots/relay   T4 ball-game/cues   T5 executor/hud   (5 builders in parallel)
wave 2   T6 page assembly (1 builder; everything above is unit-tested so this is wiring)
wave 3   T7 privacy chrome + docs   T8 OutgoingSink (C1)   T9 body layer (stretch)   + T10 human two-laptop rehearsal
```
Critical path: T0 (2.5 h) -> T4 (6 h) -> T6 (6 h) -> T7 (3 h) ~= 17.5 builder-hours. Everything demo-visible exists at T6.

## T0 — copy and wire the verified code (serial, first)

- **Files**: `sdk/ui/teams-tokens.css`, `tools/gen-teams-tokens.mjs`, `sdk/game/{prop-ball,tile-pipeline,court-map,ball-net,court-bridge,throw-detect,latency-cues,command-agent,push-to-talk}.js`, `sdk/game/tools.json`, `sdk/game/system-prompt.md`, `sdk/net/{hand-stream,remote-consumer,transports}.js`, `tools/relay-server.mjs`, `tools/sandbox-proxy.mjs`, `tests/{prop-ball,wire,ballnet,command-agent}-smoke.mjs`, NEW `sdk/game/court-space.js`, NEW `sdk/game/pack-gen.js`, NEW `tests/pack-gen-smoke.mjs`, NEW `tests/_gen-packs.mjs`, `tests/fixtures/hand-packs.json`, `docs/teams/hopeos-wire.md` (copy + the 0x21 row), `package.json` (scripts + `ws` devDependency).
- **Start from**: the copy table in `CONTRACTS.md` §1 (source paths + the only permitted edits: `tile-pipeline.js` `this.hands`, `ball-net.js` `PK.PRESENCE`, `court-bridge.js` `mirroredInput`, `transports.js` `gated()`); `court-space.js` per `CONTRACTS.md` §3.1 (constants from `R/gaps/render-budget/decision.md` §2); `pack-gen.js` from `REPO/tests/_glassprobe.mjs:36-58` generators.
- **Depends on**: nothing.
- **Acceptance (headless)**: from `REPO`: `node tests/prop-ball-smoke.mjs` prints 23 passed / 0 failed; `node tests/wire-smoke.mjs` all 7 sections pass with section (vii) run for both `mirroredInput:true` (-> `EDGE.L`) and `false` (-> `EDGE.R`); `node tests/ballnet-smoke.mjs` all sections pass; `node tests/command-agent-smoke.mjs` 114 passed; `node tests/pack-gen-smoke.mjs` passes (each generator returns 21 points, `closure(open) < 0.1`, `closure(cup) > 0.3`, `flat` has `n.y > 0.4` via `palmPose`, `packToHands` round-trips a pack through `HandViews.mirrorPoint` within 1 mm, fixtures file written); `node --check` on every file under `sdk/game`, `sdk/net`, `tools`; `node tools/relay-server.mjs 8787 &` then a `ws` client receives `{hello:{clientId}}`; `node tools/sandbox-proxy.mjs 3334 &` answers `/api/claude` with the handler's key-missing error (not a 404); `git status` shows no change under `sdk/core`, `sdk/interaction`, `mpbrowser.html`.

## T1 — twin UI shell (Fluent tokens, gallery, chrome, panel, consent, a11y)

- **Files**: `sdk/ui/teamslab.css`, `sdk/game/twin-ui.js`, `teamslab.html` (STATIC shell only: DOM skeleton of SPEC §2.4, importmap, `?shell=1` boot that mounts `TwinUI` with placeholder tiles and no tracking; T6 replaces the boot script, keeps the DOM), `tests/twin-ui-smoke.mjs`, `tests/_uiprobe.mjs`.
- **Start from**: `R/ui/layout-spec.md` §1-§7, `R/ui/scripts/gallery-grid.mjs::fixed169` (copy the function into `twin-ui.js`), `R/ui/scripts/gallery-grid-results.txt`, `R/ui/overlay-sketches.md` §5-§8, `R/ui/accessibility.md` R1-R12, `R/ui/scripts/contrast.mjs` (+ `contrast-results.txt`), `R/gaps/render-budget/layout-spec.patch.md` (stacking), `R/gaps/data-privacy-terms/consent-copy.md` (C1, C3, C4, C7, C10 verbatim), `R/gaps/data-privacy-terms/patches/layout-spec-privacy.patch.md`. API: `CONTRACTS.md` §3.14.
- **Depends on**: T0 (tokens).
- **Acceptance (headless)**: `node tests/twin-ui-smoke.mjs`: `fixed169(n, W, H)` reproduces every row of `gallery-grid-results.txt` for n = 1..9 at 994x678, 918x540, 1280x720, 1920x1080; the 8 text/background token pairs used by the CSS pass 4.5:1 (reuse `contrast.mjs`); the API-tab row model has all 12 layers of SPEC §8 with a pill in {demo, seam, probe first}. `node tests/_uiprobe.mjs` (`teamslab.html?shell=1&tiles=4`): tile DOM rects at the six reference rectangles equal `fixed169` +/- 1 px; no horizontal scroll at 472x382; every `.tile` has `role=group` + `aria-label`; control bar is `role=toolbar` with 8 buttons >= 32x32 and arrow-key focus movement; `#panel` tabs are `role=tab`; `--tw-motion` is `0` under `--force-prefers-reduced-motion`; the consent dialog is open on load without `?consent=1` and `navigator.mediaDevices.getUserMedia` spy count stays 0 until "I agree" is clicked (the shell calls a stubbed `onConsent` that invokes the spy); `?consent=1` skips it; `setDeviceOnly(true)` flips the local pill to "On-device only" and `aria-pressed`; screenshots at the six rectangles to `SHOT_DIR`.

## T2 — stage renderer, court space, remote tiles, hand sources

- **Files**: `sdk/game/stage-renderer.js`, `sdk/game/remote-tile.js`, `sdk/game/hand-sources.js`, `sdk/game/court-space.js` (owns it from here; T0's version is the starting point), `tests/court-space-smoke.mjs`, `tests/_stageprobe.html`, `tests/_stageprobe.mjs`.
- **Start from**: `R/gaps/render-budget/render-budget.html:185-213, 290-297, 415-425` (design S code, matcap), `R/gaps/render-budget/decision.md` §1-§4, `R/gaps/wire-protocol/hopeos-wire.md` §7 (receiver chain), `R/codebase/sandbox-skeleton/sdk/game/tile-pipeline.js:40-100` (mirror the chain in `RemoteTile`), `R/codebase/dropins.md` §3.1 (`initCamera`), §3.11 (`CompositeCam.useFile`), `REPO/sdk/core/hand-views.js:199-256` (`resolve`, `mirrorPoint`), `REPO/sdk/core/hand-rig.js:152-320` (`grp`, `setOccluder`). APIs: `CONTRACTS.md` §3.1, §3.3, §3.5, §3.6.
- **Depends on**: T0.
- **Acceptance (headless)**: `node tests/court-space-smoke.mjs`: `U` within 1e-4 of 1.8652; for the six rectangles and n in {1,2,3,4,6,9}: `courtToStagePx` <-> `worldFromStagePx` <-> `worldToCourt` round-trips within 1e-9; `pr === court.R*th` on both sides of every gutter; `uvCam` (built with `makeUvCamera`) maps tile-rect corners to u,v in {0,1} +/- 1e-6 through `CourtMap.worldToTile`; [M1]: a world +x step at u = 0.9 of seat k gives `EDGE.R` from `exitEdge` and `neighbourAt` = seat (k+1) mod n under `MIRRORED_INPUT=false`, and `EDGE.L` under `true`; `unmirrorHands` is an involution. `node tests/_stageprobe.mjs` (`tests/_stageprobe.html?tiles=9`, no landmarkers): `stage.contexts() === 1`; a matcap ball placed at every gutter mid-point renders the same projected radius (+/- 0.5 px, read via `stagePxFromWorld` and a 1-px column scan) on both sides; `assignLayer` puts both `rig.mesh` and `rig.occluder` on layer `1+i` (traverse check); a `RemoteTile` fed a `cup` pack (via `packToHands` -> `encode/decodeHandStream` -> `RemoteHands.read`) poses `rigR.mesh.visible === true` with `packs.hands.length === 1` and its `views !== ` any other tile's `views`; `WEBGL_lose_context.loseContext()` triggers `onContextLost` and the renderer is rebuilt (render calls > 0 afterwards); `ClipSource` with `tests/fixtures/meshy_walk_anim.json`-independent tiny generated webm (or any local video) reaches `readyState >= 2`; `KeyboardSource` writes `S.ovPacks[id].R` as a `flat` pack and turns it into `cup` while Space is down.

## T3 — room (PRESENCE, host election, seat map, clock cadence) + bots + relay test

- **Files**: `sdk/net/room.js`, `sdk/game/bot-tile.js`, `tests/bot-pass-smoke.mjs`, `tests/relay-smoke.mjs`.
- **Start from**: `R/game/examples/relay-server.mjs` (hello/joined/left, CLOCK_PING answered), `R/gaps/wire-protocol/stage.html:183-222` (host election, presence, clock), `R/game/examples/court-map.mjs::buildSeatMap`, `R/gaps/wire-protocol/ball-net.mjs:190-199, 463-476` (`encodeSeatMap`, `publishSeatMap`, `ping`, `kickoff`), `R/game/examples/smoke-test.mjs` (synthetic clients + fuzz section), `R/gaps/wire-protocol/transports.mjs:58-92` (`MemHub`), `R/gaps/wire-protocol/hand-stream.mjs:83-118`. APIs: `CONTRACTS.md` §4.1, §3.10.
- **Depends on**: T0.
- **Acceptance (headless)**: `node tests/bot-pass-smoke.mjs`: 4 `Room`s + 3 `BotTile`s + 1 plain BallNet client (seat 0, host, `kickoff`) over `MemHub` linked at 90 +/- 30 ms, 5 % loss, virtual clock: PRESENCE converges to roster size 4 on every client within 3 s sim; host = lowest clientId everywhere; seat map `v` increments on every join and again when a bot is pruned after 6 s silence; `clockReady()` true on the host immediately and on the others after 8 pongs (host answers pings on Mem); the ball visits every seat within 20 s sim (every bot's `stats.claims >= 1`); at every sampled 50 ms tick exactly one client has `net.isOwner && !net.inTransit` or one is in transit; `conflicts === 0` on all; every bot HAND_STREAM decodes on the host with `ageMs < 200` and `hands.right.img.length === 21`; `RemoteHands.onMessage(presenceBuf) === false` and `BallNet` ignores it [W2]. `node tests/relay-smoke.mjs`: starts `tools/relay-server.mjs` on a free port, two Node `ws` clients wrapped as `WsRelayTransport`-shaped transports exchange PRESENCE + HAND_STREAM with `ageMs < 50` on localhost and each gets 8 clock samples with `|offset| < 10 ms`; server exits cleanly.

## T4 — ball game: phase machine, cues, predictor, sfx

- **Files**: `sdk/game/ball-game.js`, `sdk/game/latency-cues.js` (append `ArrivalPredictor`, `MissCue`, `dashed` flag on `ArrivalRing`), `sdk/game/sfx.js`, `tests/ball-game-smoke.mjs`.
- **Start from**: `R/gaps/wire-protocol/hopeos-wire.md` §4/§7, `R/gaps/wire-protocol/court-bridge.mjs` (whole file), `R/gaps/wire-protocol/ball-net.mjs:263-335` (`tickOwner`, `respawn`, `tick`), `R/game/examples/latency-cues.mjs`, `R/game/examples/throw-detect.mjs:20-30, 106-125` (`THROW`, statics), `R/game/examples/court-map.mjs:205-224` (`predictExit`), `R/game/mechanics-options.md` §1a/§1b/§3, `R/game/latency-feel.md` §3-§5, `R/game/sync-protocol.md` §4/§6, `R/codebase/sandbox-skeleton/sdk/game/prop-ball.js:261-400` (`update`, `onRelease`, `onExit`), `design/game-feel-first/DESIGN.md` §7.1-§7.5 (phase table, FEEL). APIs: `CONTRACTS.md` §3.7-§3.9; rules SPEC §6.
- **Depends on**: T0.
- **Acceptance (headless)**: `node tests/ball-game-smoke.mjs` (Node, `scene=null`, two headless `BallGame`s at seats 0/1 (+1 bot-less proxy seat 2 untracked) over `MemHub` 120 +/- 30 ms, 5 % loss, `court-space` mapping from `fixed169(3, 994, 678)`, packs from `PackGen`): (a) seat 0 kicks off -> phase `floor`; `flat` under the ball -> `cradle` and phase `held`; `cup` -> `holdType 'wrap'`; moving the cup pack +x at 1.5 m/s for 20 frames then `open` -> phase `flight` then `transit` (LAUNCH seen on seat 1 with `to === 1`) then seat 1 `arriving` at `tEdgeEff` +/- 1 frame, entry speed <= 2.0 u/s, `vel.x > 0` (entered from the left), `pos.z === -D`; (b) [P1] `ArrivalPredictor` on seat 1 reports `leadMs >= 300` before the LAUNCH and `predicted` flips false after it, in >= 80 % of 40 fuzzed throws (random release speed 0.5-2.5 u/s, angle -20..30 deg); (c) [L1] `classifyRelease` drop / throw / clamped / lost thresholds exact; a hold whose pack vanishes yields `|vel| <= 0.3*U` and the ball stays in the tile (no LAUNCH within 2 s); (d) [D2]/[D3]/[D5] re-asserted through `BallGame.tick` (open hover 30 frames: no grab, `|dy| < 1 mm`; open after cup: released that frame; flat support: `y >= palm.y + r - 3 mm`); (e) goal: seat 1 designated goal, free ball launched into the ring while seat 1's packs are empty -> GOAL on both, `score[0] === 1`, phase back to `respawn` -> `floor` at kickoff; a `flat` pack in front of the ring -> no goal, `stats.saves === 1`; (f) potato: fuse restarts on each CLAIM and at expiry a GOAL with bit 7 set arrives on both, loser = owner seat; (g) practice (n = 1, `wrap:'none'`): a +x throw bounces (`onWall`, `vel.x < 0` afterwards), never LAUNCHes; (h) phases never skip `arriving`; `dt` is clamped ([1/120, 1/30]); (i) `sfx.js` `node --check` and `textTwin(name)` non-empty for every cue; (j) greps [D1][D4] over `ball-game.js`, `latency-cues.js`.

## T5 — scene executor + HUD

- **Files**: `sdk/game/scene-executor.js`, `sdk/game/hud.js`, `tests/scene-executor-smoke.mjs`.
- **Start from**: `R/command-agent/mock-executor.js` (contract + wiring notes), `R/command-agent/NOTES.md` (executor contract), `R/command-agent/tools.json` (enums; load it in the test), `R/codebase/sandbox-skeleton/sdk/game/sandbox-tools.js:63-91`, `R/codebase/agent-tools.md`, `R/codebase/sandbox-skeleton/sdk/game/prop-ball.js:44-58, 199-226` (`palmPose`, `_holdPose` pocket maths), `REPO/sdk/core/game-physics.js:54-210` (`HandBody.openness`), `R/gaps/wire-protocol/hopeos-wire.md` §5 (HUD ages), `R/game/latency-feel.md` §4 (bands), `R/meeting-app/stage.html` HUD section (hz/p50/p95), `R/codebase/test-conventions.md` §3-§4 (AUTOTEST). APIs: `CONTRACTS.md` §3.12, §3.13.
- **Depends on**: T0.
- **Acceptance (headless)**: `node tests/scene-executor-smoke.mjs` (Node, `scene = new THREE.Scene()`, packs from `PackGen`, a fake local tile `{packs, handL, handR}` with `HandBody.openness` set from the pack): [X3] every tool name in `tools.json` has a branch and every `attach`/`behavior` enum value returns an object with boolean `ok`; [X1] `spawn_object {kind:'apple', attach:'right_hand'}` with an `open` right pack -> prop centre within 2 mm of `palm + n*(r+0.01)` with `n` from the measured normal (flip the pack's z and the normal sign must flip too); `either_hand` with no open hand -> `ok:true, attach:'world_front'`; [X2] removing the pack -> `vy < 0` within 10 frames; a `cup` pack around the spawned apple -> `holdType 'wrap'` within 10 frames; [D8] `mesh.parent === scene`; [D4] `sphere.gravity < 0` for every physics preset; [D6] `seek_hand` for 60 frames with the hand 0.3 m above -> prop `y` never above its start; `orbit_hand` -> `ok:false` with a reason string; `transform_object {scale:10}` clamps radius to 1.0; `remove_object 'all'` empties the map; [X4] `pass_object` on an apple -> `ok:false`; on the game ball with a stub game not owner -> `ok:false`; `designate_goal` with `isHost:false` -> `ok:false`; `list_scene` returns the snapshot shape `CommandAgent._snapshot` reads; then `node tests/command-agent-smoke.mjs` still 114/114 and a `CommandAgent({mode:'local', executor})` run of "give me an apple in my hand" produces one `spawn_object` action with `source 'local'`. `hud.js`: `node --check`; a unit block in the same test feeds 300 frames and checks p50/p95 and `band()` thresholds (80/150/300/500).

## T6 — page assembly (boot, loop, URL params, wiring, probes)

- **Files**: `teamslab.html` (boot + loop script; DOM from T1 kept), `tests/_twinprobe.mjs`, `tests/_twinpass-probe.mjs`, `package.json` (`probe:*` scripts).
- **Start from**: `R/codebase/minimal-holohands.html` (boot order), `R/codebase/dropins.md` §2, §5, `R/codebase/test-conventions.md`, `REPO/tests/_glassprobe.mjs` (probe skeleton, `ovPacks` injection pattern), `R/codebase/_probe-minimal.mjs`, `CONTRACTS.md` §5-§6 (page contract, per-frame flow), all wave-1 module APIs.
- **Depends on**: T1, T2, T3, T4, T5.
- **Acceptance (headless)**: `node tests/_twinprobe.mjs` (`teamslab.html?auto=1&fixedstep=1&consent=1&transport=loopback&tiles=3&clone=1&bot=1&agent=local&nosfx=1`): (1) 4 tiles laid by rule B in seat order, sizes = `fixed169(stageW, stageH, 4)`; (2) `stage.contexts() === 4` (renderer + 3 hand landmarkers), `info().calls > 0`; (3) over 5 s `AUTOTEST` p50 `scriptMs < 45`, fps >= 25 under fixedstep; (4) `hud.remote[bot] < 100 ms`, bot tile `rigR.mesh.visible`, local tiles `packs.hands.length === 0` (fake camera), no console errors beyond the filter; (5) remote tile `views` distinct from the local tile's; (6) doctrine via `ovPacks[me]`: `open` over the ball 30 frames -> `held === null`, `|dy| < 1 mm`; `cup` -> `held` within 10 frames with `holdType in {wrap, clip}`; `open` -> released that frame; `flat` under -> `cradle` and `y >= palm.y + r - 3 mm`; (7) occlusion: after a `cup` pose the conform delta at a contact joint > 0 (`rig.pose(pack, [ball.collider])`); (8) `agent.cmd.command('give me an apple in my hand')` with an `open` right pack -> `props.size === 1`, prop within r + 0.01 of the palm, `source === 'local'`, `mesh.parent === stage.scene`; (9) keyboard: `K`, arrows, Space -> `ovPacks[me].R` present and the ball picked up by wrap; (10) a11y: `#live-polite` changed after the pickup; `--tw-motion === '0'` under `--force-prefers-reduced-motion`; every `.tile` has `aria-label`; (11) [G1] `btnDeviceOnly` -> `transport.stats.sent` stops increasing, `suppressed` increases, agent mode `local`; (12) screenshots at the six rectangles. `node tests/_twinpass-probe.mjs` (two pages, same origin, `?auto=1&fixedstep=1&consent=1&transport=loopback&room=t&name=A&host=1` and `...&name=B`): both `seatMap.v >= 1` with identical seat order; A: `flat` then `cup` -> held; move +x 1.5 m/s 20 frames, `open` -> B sees LAUNCH (`net.stats.launches` on A = 1), B's predictor showed `leadMs >= 300` before it, B `onBecameOwner` within `tEdgeEff + 250 ms`, B ball inside B's tile with `vel.x > 0`, A `inTransit === false` afterwards, exactly one owner at every 50 ms sample over 3 s; `designate_goal` from A's agent ("make B the goal") -> `seatMap.goal === 1` on both; throw into the ring with B's hands empty -> GOAL on both, `score[0] === 1`, live regions announce on both; hot potato -> fuse on both, no goal ring; `?bot=1` on A -> the bot appears in both rosters and `stats.claims >= 1` within 20 s.

## T7 — privacy chrome finish, API tab live numbers, docs, run book

- **Files**: `teamslab.html` (About dialog, C7 voice notice, outgoing help), `sdk/game/twin-ui.js` (API tab `live()` wiring), `docs/teams/*` (copies of every file linked from SPEC §8), `TEAMSLAB.md`, `tests/_uiprobe.mjs` (extend).
- **Start from**: `R/gaps/data-privacy-terms/consent-copy.md` (C7, C10, C11, C13), `privacy.html`, `terms.html`, `licences.md` §4, `R/gaps/data-privacy-terms/data-flow.md`, SPEC §8 table (links + blockers), `REPO/WORLD_HOSTING.md` (run-book style).
- **Depends on**: T6.
- **Acceptance (headless)**: `node tests/_uiprobe.mjs --links`: every `href` in the API tab resolves 200 on :3333; each row shows a `live()` string that changes between two 1 s samples for rows with live data (HUD, remote hands, ball); strings in the consent dialog, pills, device-only button, strip and About footnote byte-match `consent-copy.md` C1/C3/C4/C10; `TEAMSLAB.md` commands (`npm run serve`, `npm run relay`, `npm run proxy`, the four URLs) are copy-pasteable and `npm run test:twin` is green.

## T8 — OutgoingSink crop popup (C1 via OBS)

- **Files**: `sdk/net/video-sink.js`, `teamslab.html` (`?outgoing=1` popup mode + More-menu entry), `tests/_outgoingprobe.mjs`.
- **Start from**: `R/acs/acs-client.html:233-274` (compositor -> `LocalVideoStream`), `R/video-effects/effect-core.js:44-56`, `R/gaps/render-budget/decision.md` §4 (context budget: no second renderer). API: `CONTRACTS.md` §4.2.
- **Depends on**: T6.
- **Acceptance (headless)**: `node tests/_outgoingprobe.mjs`: with `?auto=1&consent=1&fixedstep=1&outgoing=1` the sink canvas is 1280x720, `stream.getVideoTracks()[0].readyState === 'live'`, `stats.fps >= 20` over 3 s, the centre pixel is non-black, the video is drawn UN-mirrored (a marker drawn at world +x in the surface appears on the LEFT of the sink canvas), the popup window paints (non-black centre pixel via a second page on `?outgoing=1&sinkview=1`), and `stage.contexts()` is unchanged by enabling the sink.

## T9 — body layer (stretch): pose on the local tile + body support for props

- **Files**: `sdk/game/body-layer.js`, `sdk/game/prop-ball.js` (`extraBodies` per `CONTRACTS.md` §3.11), `tests/prop-ball-smoke.mjs` (24th check), `teamslab.html` (Body toggle wiring), `tests/_twinprobe.mjs` (+ body section).
- **Start from**: `R/codebase/dropins.md` §3.7, §3.10 (handlab `:937-984` recipe: `mirrorPoint` + `BodyPose.stabilizeMirror` + 33-bank), `REPO/sdk/core/game-physics.js:951-1040` (`BodyBody`, `BODY_RADII`), `REPO/sdk/core/body-rig.js`, `R/codebase/sandbox-skeleton/sdk/game/prop-ball.js:353-368` (the support loop to mirror for bodies).
- **Depends on**: T6.
- **Acceptance (headless)**: `node tests/prop-ball-smoke.mjs` 24 passed (the 23 originals unchanged + [D7]: a body sphere under the ball supports it, ball never inside it, never grabbed); `node tests/_twinprobe.mjs --body`: with `&body=1` `stage.contexts() === 5`, a synthetic 37-point pose injected through a `S.ovBody` seam places the chest capsule under the ball and the ball rests on it (y stable +/- 3 mm over 60 frames); script p50 still < 45 ms.

## T10 — human two-laptop rehearsal (not a builder task)

Run SPEC §1.1 over Wi-Fi with `npm run relay` on A; tune `rateHz` (20 vs 30) and `HANDOFF_TIMEOUT_MS` (250 vs 400) from
HUD numbers; record own age, remote age, relay RTT, handoff p95, predictor lead into `TEAMSLAB.md`. Done when every beat
reproduces twice in a row and the HUD shows own age < 60 ms, remote age < 120 ms on LAN, handoff p95 < 300 ms.

## Parallel grouping summary

| wave | tasks | builders |
|---|---|---|
| 0 | T0 | 1 |
| 1 | T1, T2, T3, T4, T5 | 5 |
| 2 | T6 | 1 |
| 3 | T7, T8, T9 (+ T10 human) | 3 |

Merge rule between waves: each wave-1 task owns disjoint files (see lists); T6 is the only task that edits `teamslab.html`
after T1 and it keeps T1's DOM; T7/T8/T9 touch `teamslab.html` in separate, marked blocks (`<!-- T7 -->` etc.) and must
re-run `npm run probe:twin` before finishing.
